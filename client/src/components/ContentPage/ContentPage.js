import React from 'react'
import { ContentPageContainer, UpperContainer } from './ContentPageElements'

const ContentPage = () => {
    return (
        <ContentPageContainer id="content">
        </ContentPageContainer>
    )
}

export default ContentPage
